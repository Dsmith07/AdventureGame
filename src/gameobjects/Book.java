package gameobjects;

public class Book extends Treasure{
    public Book(String aName, String aDescription, int aValue, ThingHolder aContainer) {
        super(aName, aDescription, aValue, aContainer);
    }
}
